**Waitlist page — mobile — revision**

Mobile only. Remove the white containers around the form. Everything sits directly on the page background, single column, same horizontal margins as the survey screens.

Stack, top to bottom:

1. **Clip** `upcoming-reminder-cards.mp4` —  Play once, hold last frame. Reduce top spacing so the clip starts near the header.
2. **Heading** — *Join the waitlist.* Serif, same style as Q1 heading.
3. **Subtext** — *Stop holding every unfinished task inside your head.*
4. **Email field** — full width, single border, placeholder *you@email.com*. Remove the "Email" label.
5. **Primary button** — label *Join the waitlist* (exact match to heading; remove "Beta"). Same width as the field. Use the Q1 selected-state dark green, not the current grey-green.
6. **Supporting line** below the button, muted — *Early access is small on purpose. A few households at a time.*
7. **background**Clip's background can't be transparent. color-match the page background to #F0F0EE.

Remove the two-line privacy disclaimer. 

No other visual changes.